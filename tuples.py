#Tuples

#Tuple are immutable ie.. they can't be modified which is no new items can be added or removed
#tuples remain constant

numbers = (2,5,8,12)

print(numbers[1])

numbers[2] = 13
print(numbers)